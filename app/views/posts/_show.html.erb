<h1>Hello</h1>
<%= image_tag post.post_image_id.to_s, size: '500x500', class: 'photo-img', id: 'photo-img' %>
<div id="follow_form">
  <%= render 'relationships/follow_button', user: user %>
</div>
<div class="favorite_btn_<%= post.id %>">
  <%= render 'favorites/favorite_btn', post: post, favuser: favuser %>
</div>