<div class="col-12 mt-3 mb-3">
  <% if obj.errors.any? %>
    <div id="error_explanation">
      <div class="error-text">
        <h5>入力に誤りがあります。入力内容を確認してください。</h5>
        <ul>
          <% obj.errors.full_messages.each do |message| %>
            <li>・<%= message %></li>
          <% end %>
        </ul>
      </div>
    </div>
  <% end %>
</div>

